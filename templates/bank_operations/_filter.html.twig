

{{ form_start(form,
            {
                attr: {class: 'form-group filter-sticky'},

            }
) }}

{{ form_row(form.q) }}

<h4>Categories</h4>

{{ form_row(form.categories) }}

<h4>Type</h4>
{{ form_row(form.type) }}


{{ form_row(form.min) }}
{{ form_row(form.max) }}

<button type="submit" class="btn btn-primary w-50 ">Filter</button>
{{ form_end(form)}}

